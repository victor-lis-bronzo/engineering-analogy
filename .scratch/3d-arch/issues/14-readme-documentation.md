# 14: Documentação Funcional do README e Roteiro de Apresentação

**What to build:** Criar o arquivo `README.md` completo, funcional e claro na raiz do repositório, documentando a função acadêmica e didática do projeto, o roteiro detalhado das fases e analogias conceituais com Clean Architecture e SOLID, o modo de prototipagem com variações estruturais (`/prototype` com variantes A, B e C), a arquitetura técnica e o guia de execução.

**Blocked by:** 13

**Status:** resolved

## Acceptance criteria (TDD & Metrics)
- [x] O arquivo `README.md` existe na raiz do repositório com formatação Markdown impecável e sem links quebrados.
- [x] Contém a explicação clara do problema e da função do projeto (metáfora física do prédio vs. dívida técnica de software).
- [x] Contém o roteiro fase a fase (Fases 0 a 6) descrevendo a narrativa visual, o texto exibido e o princípio de software correlato.
- [x] Contém um guia prático de apresentação oral / roteiro de pitch para condução da demonstração interativa.
- [x] Documenta detalhadamente o modo `/prototype`, o parâmetro de URL `?prototype=true` e a especificação das 3 variantes estruturais (Variante A, B e C).
- [x] Documenta a arquitetura técnica do código (Zustand, R3F, Three.js, Rapier, Tailwind).
- [x] Documenta as instruções completas de instalação, execução, testes e build (`npm install`, `npm run dev`, `npm test`, `npm run build`).
- [x] A suíte de testes automatizados (`npm test`) passa com 100% de sucesso e a compilação (`npm run build`) termina com 0 erros e 0 warnings.
